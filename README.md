# Train Wreck Server


Can ensure you have the latest version of the Docker Image

```bash
docker compose pull
```

Can run it via

```bash
docker compose up -d
```

Can tear it down

```bash
docker compose down
```
